// for

for (let i = 0; i <= 10; i++) {
    const element = i;
    //console.log(element);


}

// While
let index = 0
while (index <= 10) {
    //console.log(`value of index is ${index}`);
    index = index + 2;


}

let myArray = ['gan', 'ran', 'jan']
let arr = 0
while (arr < myArray.length) {
    console.log(`value is ${myArray[arr]}`);
    arr = arr + 1;
}